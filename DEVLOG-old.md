# EVE Idle 开发日志

## 2026-07-04（第 1 天）

### 已完成
- [x] 项目初始化（PROJECT.md + index.html 框架）
- [x] 界面骨架（侧边栏 + 主面板 + 状态栏）
- [x] 数据结构（gameState + 18 个技能）
- [x] 采矿逻辑（基础产出 + 经验增长）
- [x] 冶炼逻辑（原矿 → 矿物转换）
- [x] 行星农耕（后台产出 + 收取功能）

### 今日决策
- 仓库系统 → 独立页面，优先于战斗实现
- 多采矿目标 → 根据等级解锁不同矿带
- 战斗系统 → 延后至生产链和仓库完善后再做
- 货柜管理 → 影响仓库容量

### 待完成
- [ ] 阶段 4：仓库容量限制 + 满仓停产
- [ ] 阶段 5：多采矿目标切换
- [ ] 阶段 6：仓库页面（可视化库存）

### 明天计划
1. 实现仓库容量限制（阶段 4）
2. 实现多采矿目标切换（阶段 5）
3. 实现仓库页面（阶段 6）
4. 视情况开始制造系统

### 遇到的问题
- CodeWhale 单次生成长代码会卡死，需分阶段执行。
- 解决方法：每次只发 200~400 行代码粒度的指令。

### 当前代码状态
- 文件：index.html
- 备份：index_backup.html
- 已实现：采矿、冶炼、行星农耕（基础版）
- 待实现：仓库系统、多目标切换、仓库页面

## 2026-07-05（第 2 天）

### 已完成

- [x] 采矿系统全面升级
  - 多矿石目标切换：下拉菜单，7 种矿石按等级解锁，未解锁灰色斜体
  - 效率驱动倒计时：`采矿效率 = 1 × (1 + lvl × 0.02)`，hover 显示公式
  - 数据表更新：baseTime/baseXP 调整为 ×10 倍（20~380s），初始采矿等级 86
  - 手动开始采矿，切换页面不停止运行中技能
- [x] 冶炼系统全面重写
  - 旧版逐秒消耗 → 新版倒计时模式，与采矿完全对称
  - 7 条 SMELTING_RECIPES 配方表（消耗矿石 → 产出矿物）
  - 原料不足/仓库满自动暂停，手动开始冶炼
  - 初始冶炼等级 1
- [x] 仓库系统
  - 顶部状态栏容量指示器（进度条 + 文字）
  - 容量公式：`1000 + 货柜管理Lv × 100`
  - 独立仓库页面：分类标签（矿石/矿物/行星产物/消耗品）+ 物品网格
- [x] 存档系统
  - SaveAdapter → LocalStorageAdapter → SaveManager 三层架构
  - 脏标记 5 秒自动保存，beforeunload 页面关闭保护
  - 导出/导入 JSON 文件，页面加载自动恢复
  - 预留 ServerAdapter 接口注释
- [x] 面板分离
  - `currentView`（显示面板）与 `currentAction.skill`（运行技能）分离
  - 切换面板不停止运行中技能
  - 顶部活动指示器实时反映运行状态
- [x] 平滑进度条
  - `progress + lastProgressUpdate` 追踪 + `requestAnimationFrame` 60fps 插值
  - delta time 推进，while 循环产出，进度从 0% 连续增长到 100%
- [x] Bug 修复
  - 下拉菜单 z-index 低被面板遮挡
  - 切换页面时进度条误用另一技能的 progress
  - 同一技能重复点击重置进度

### 今日决策
- 采矿/冶炼经验 ×10 以缓解 1 秒 tick 间隔的进度条跳动问题
- 面板与运行技能解耦，允许查看仓库/存档时后台继续生产
- 冶炼改为手动启动，与采矿行为一致

### 当前代码状态
- 文件：index.html（~1790 行，含 ~700 行 JS）
- 备份：index_backup.html
- 已实现：采矿、冶炼、仓库、存档、平滑进度条、面板分离
- 待实现：制造系统、行星页面、战斗系统

## 2026-07-06（第 3 天）

### 已完成

- [x] 离线收益计算系统
  - `lastActiveTime` 时间戳追踪（gameTick + renderLoop 双重更新）
  - 页面加载离线补算（`calculateOfflineGains` → `applyOfflineGains`）
  - 浏览器 Tab 切回前台自动补算（`visibilitychange` 事件）
  - 离线时长上限 24h，受仓库容量 + 资源库存双重约束
  - 采矿/冶炼离线产出 + 气体/行星/探索离线预留接口
  - 金色 Toast 提示离线结算结果，4 秒自动消失
  - `forceOfflineTest(秒数)` 控制台调试函数（技能未运行自动激活）

- [x] 行星开发系统（行星农耕）
  - 6 种行星类型：熔岩(1)/气态(1)/冰(20)/等离子(40)/温带(60)/风暴(80)
  - 独立页面（侧边栏 🪐 行星概览），与主动技能互不干扰
  - 部署/收取/续期完整流程，部署弹窗（模态）
  - 后台产出（`planetaryTick`，始终运行不占主动槽）
  - 槽位随技能等级解锁（`1+floor(Lv/10)`，最多 5 个）
  - 行星独立仓库（100 + Lv×5），满仓停产
  - 产出速度受技能加成（`间隔 / (1 + Lv×0.02)`）
  - 部署周期 24h，到期需续期
  - 离线补算集成，旧存档自动迁移
  - 测试部署费用：0 ISK + 1 三钛合金

- [x] 行星产出倒计时
  - 每个行星卡片新增绿色产出进度条（`planet-prod-row`）
  - renderLoop 60fps 平滑动画（轻量更新，不重建 DOM）
  - 实时显示倒计时秒数（`~3.2s`）

- [x] 气体采集系统
  - 7 种气体区域：富勒烯云团(1) → 超纯聚合气体(85)
  - 与采矿完全对称的效率公式 + 进度模式 + while 逐次产出
  - 独立下拉菜单 + 进度条 UI（复用采矿样式）
  - 侧边栏 ☁️ 气体采集入口
  - 切换按钮改为三向轮转（采矿 → 冶炼 → 气体）
  - 离线补算 + 仓库「气体」分类标签
  - 旧存档向后兼容（`gasHarvesting` 技能 + `gases` 资源自动补全）

- [x] Bug 修复
  - 行星收取按钮无法点击 → renderLoop 改为每秒刷新（不再 60fps 重建 DOM）
  - `forceOfflineTest` 增加 Toast 弹窗 + 技能未运行自动激活

### 今日决策
- 行星产出改为 progress 累积模式（和采矿同架构），支持倒计时显示
- 气体采集对标采矿而非梅尔沃钓鱼模式（效率 + 进度条）
- 测试阶段仓库容量固定一千万，部署费用 0 ISK
- 行星页面用 gameTick 每秒刷新，renderLoop 仅做进度条宽度轻量更新

### 当前代码状态
- 文件：index.html（~1970 行，含 ~1100 行 JS）
- 已实现：采矿、冶炼、气体采集、行星开发、仓库、存档、离线补算、平滑进度条、面板分离
- 待实现：制造系统、战斗系统、弹药工程、舰船工程、装备工程、改装件工程、逆向工程

### 明天计划
1. 战斗系统框架（异常空间、伤害属性、每秒结算）
2. 弹药工程（制造消耗品支撑战斗）
3. 或根据策划案实现制造体系 T1 层

### 遇到的问题
- renderLoop 每帧重建 DOM 导致事件监听丢失 → 改用 gameTick 每秒刷新页面 + renderLoop 仅更新进度条宽度
- 无

## 2026-07-07（第 4 天）

### 代码规模
- **index.html**：1983 行 → 2978 行（**+995 行**，+50%）
- **JS 逻辑**：~1100 行 → ~1820 行（**+720 行**）

---

### 一、新增系统（6 项）

#### 1. 舰船工程系统（T1 层）
- 技能 shipEngineering（Lv.1），效率 `1×(1+Lv×0.02)`
- 6 种 T1 部件配方，消耗矿物 + 行星产物
- 3 艘新手战舰：裂谷级（激光/护盾）、茶隼级（导弹/装甲）、阿特龙级（炮台/结构）
- 属性完全沿用策划案第 7 节 STARTER_SHIPS
- 蓝图商店：T1 永久蓝图 50,000 ISK
- 部件制造 + 舰船合成双子模式，共享技能槽
- 离线补算完整集成

#### 2. 执行确认弹窗
- 点击"开始"→ 弹窗显示单次耗时、资源需求（含库存）、单次产出
- 采集类：单次耗时 + 产出
- 消耗类：单次耗时 + 资源需求（库存） + 产出
- 支持输入执行次数，实时显示总耗时
- Enter 快捷确认

#### 3. 批量执行机制（batchRemaining）
- 输入 N 次执行 N 次后自动停止
- ∞ 无限模式（batchRemaining=-1 永不递减）
- 进度条停止后正确归零（修复 updateUI 缺少 active 检查的 bug）

#### 4. 动作队列系统
- 数据结构：gameState.queue（items/config/status）
- 添加/删除/↑↓排序/清空/循环模式
- 当前任务完成后自动推进到下一项
- 同技能+目标自动合并 count（300 次采集显示为 1 行而非 300 行）
- 确认弹窗自动加入队列首位并启动

#### 5. 面板标题栏 UI 统一
- 采矿/冶炼/气体/舰船工程：左侧图标+名称，右侧"进行中"/"待命"
- 与顶部活动指示器分离（活动栏始终显示运行中技能）

#### 6. ISK 测试资金
- 初始 ISK：1,000,000
- 旧存档迁移时 ISK < 10,000 自动补充

---

### 二、Bug 修复（6 项）

| # | Bug | 修复 |
|---|-----|------|
| 1 | 停止后进度条仍涨满 | updateUI 进度条计算加 `active` 条件 |
| 2 | confirmAction 确认后不执行 | `_actionConfirmTarget` 在 hide 之前保存 |
| 3 | 弹窗确认后面板不切换 | confirmAction 显式设置 currentView |
| 4 | ISK 被旧存档覆盖为 0 | 存档迁移检测 ISK < 10000 补充 |
| 5 | 输入次数后总耗时不变 | `addEventListener({once})` → `oninput` |
| 6 | 部件/合成配方数据错误 | 与策划案对齐：三钛 ×2000→×20，部件×1→×10 |

---

### 三、今日决策
- 舰船部件配方按策划案修正（大幅降低三钛消耗）
- 舰船合成：部件×10 + 三钛×100，无拷贝费
- 蓝图拷贝/发明/研究延后
- 队列用 ↑↓ 按钮替代拖拽排序（纯 HTML 限制）
- 离线队列补算第一期暂不做

### 当前状态
- **已实现**：采矿、冶炼、气体采集、行星开发、舰船工程、仓库、存档、离线补算、执行确认弹窗、动作队列
- **待实现**：战斗系统、弹药/装备/改装件/逆向工程

### 明天计划
1. 战斗系统框架
2. 或弹药工程

## 2026-07-08（第 5 天）

### 代码规模
- **index.html**：2978 行 → 3612 行（**+634 行**，+21%）
- **JS 逻辑**：~1820 行 → ~2400 行（**+580 行**）

---

### 一、新增系统（3 项）

#### 1. 战斗系统（阶段 1-3）
- 三武器体系：激光（电磁/DPS120）、导弹（爆炸/DPS100）、炮台（动能/DPS80）
- 三海盗集团 × 各 3 种敌人：天使（护盾）、血袭者（装甲）、萨沙（结构）
- 伤害公式 `命中^1.4 / (命中^1.4 + 闪避^1.4)` + 克制 1.25×
- HP 穿透（护盾→装甲→结构）+ 每秒结算 combatTick
- 维修装备：护盾回充器/装甲维修器/结构修理器，可独立开关
- 3 个异常空间（天使前哨站/血袭者隐蔽所/萨沙哨站）
- 击杀掉落 ISK + LP，无限波次递增难度
- 爆船后返回船坞修复，停止按钮恢复满血
- HP 显示格式：`当前值 / 最大值 (百分比)`
- 战斗中显示"第 X 波敌人"

#### 2. 燃料 + 弹药系统
- `resources.fuel`（燃料池）+ `resources.ammunition = { laser, missile, cannon }`
- 燃料/弹药不占仓库容量，战斗面板实时显示库存
- combatTick 消耗逻辑：激光（燃料-3, 弹-1）、导弹（燃料-1, 弹-1）、炮台（燃料-2, 弹-1）
- 维修消耗：护盾回充（燃料-1）、装甲维修（燃料-1）、结构修理（燃料-3）
- 燃料归零 → 攻击+维修停止；弹药归零 → 攻击停止

#### 3. 弹药工程制造面板
- 侧边栏 💥 弹药工程入口（`data-skill="ammunitionEngineering"`）
- 5 条配方：标准燃料单元/强化燃料单元/激光晶体/导弹/炮台弹药
- 效率公式 `1×(1+Lv×0.02)`，对标舰船工程架构
- 下拉配方选择、开始制造、进度条、确认弹窗、队列集成

---

### 二、配套改动

#### 仓库系统增强
- 消耗品标签新增：燃料单元、激光晶体弹药、导弹、炮台弹药
- 装备标签新增：6 种舰船部件（船体骨架/推进系统/核心系统等）从 `shipComponents` 读取
- `getCargoUsed` 不统计燃料/弹药（无限容量）

#### 船坞系统
- 侧边栏 🚢 船坞入口（`data-page="hangar"`）
- 舰船卡片展示：属性、加成、装备/修复按钮
- 旧存档自动赠送裂谷级护卫舰
- 修复按钮：一键恢复当前舰船 HP 至满血

#### `hasEnoughMats` / `deductMats` 重构
- 材料检查扩展为遍历全部 4 个资源池（minerals/ores/planetary/gases）
- 旧版仅查 minerals + planetary，导致气体配方永远判定材料不足

---

### 三、Bug 修复（8 项）

| # | Bug | 修复 |
|---|-----|------|
| 1 | 队列消耗型行动资源不足时卡住不跳转 | 新增 `stopOrSkip()`，11 处失败点统一跳转 |
| 2 | 船坞显示为空 | 迁移代码追加 `ships.length===0` 赠舰 |
| 3 | 战斗页面点不开 | 迁移代码补 `combat`/`missileOperations` 技能 |
| 4 | `ITEM_ICONS` 缺少逗号导致 JS 崩溃 | 补逗号 |
| 5 | `ITEM_CATEGORIES` consumable 键重复 | 清理旧行 |
| 6 | 弹药工程侧边栏和面板 HTML 静默丢失 | 重新插入 `#ammoeng-panel` + nav-item |
| 7 | 弹药工程下拉菜单被 `overflow:hidden` 遮挡 | `#ammoeng-panel { overflow: visible }` |
| 8 | 燃料制造不起效（`hasEnoughMats` 不查 gases） | 扩展为 4 池检查 |

---

### 四、今日决策
- 弹药工程直接一步制造（对标舰船工程，不搞部件+成品双子模式）
- 燃料/弹药无限容量，不占仓库——简化资源管理
- 战斗技能（炮术/防御等）已声明但暂不生效，优先跑通燃料→弹药→战斗闭环
- 安全等级仅实现 1.0-0.8 一层，其余 4 层延后
- HP 显示改为 `当前/最大 (%)` 三合一格式，替代纯百分比

### 当前状态
- **已实现**：采矿、冶炼、气体采集、行星开发、舰船工程、弹药工程、仓库、存档、离线补算、执行确认弹窗、动作队列、**战斗系统**、**燃料/弹药系统**、船坞
- **待实现**：战斗技能生效、装备工程、改装件工程、逆向工程、LP 商店、5 层安全等级、离线战斗、精英/Boss 敌人

### 明天计划
1. 战斗技能生效（炮术/防御等接入 combatTick 数值计算）
2. 或装备工程制造系统

## 2026-07-09（第 6 天）

### 代码规模
- **index.html**：3612 行 → 4083 行（**+471 行**，+13%）

---

### 一、战斗技能系统全面接入（核心）

#### 技能拆分与新增
- `gunnery` 拆分为 `laserOps`（激光操作）和 `cannonOps`（炮台操作），三武器各自独立技能体系
- 新增 `piloting`（操船）：每级 +1 闪避值
- 删除 `electronicWarfare`（电子战）
- 最终 10 项战斗技能全部接入 `combatTick` 数值计算

#### 技能加成公式
| 技能 | 效果 | 公式 |
|------|------|------|
| 激光操作 / 炮台操作 / 导弹操作 | +命中 +伤害 | +4 命中/级, +2% 伤害/级 |
| 锁定速度 | +通用命中 | +3 命中/级 |
| 护盾操作 / 装甲加固 / 结构工程 | +对应层 HP 容量 | +3%/级 |
| 操船 | +闪避 | +1/级 |
| 电容管理 | -燃料消耗 | `1/(1+Lv×0.02)` |
| 防御 | +维修效率 | `1+Lv×0.02` |

#### 经验获取映射（行为驱动）
| 触发行为 | 获得经验的技能 |
|---------|-------------|
| 发射武器 | 对应武器技能 +2、锁定速度 +1 |
| 消耗燃料 | 电容管理 +0.3/每燃料 |
| 护盾/装甲/结构受损 | 对应防御技能 +1、操船 +1 |
| 触发维修 | 防御 +1 |
| 击杀敌人 | combat +xpDrop |

#### CL（战斗总等级）
- `CL = floor((max(三攻击) + max(三防御)) / 2)`
- 用于安全等级星带门槛（待实现）

#### 配套改动
- `WEAPON_CONFIG` 每个武器新增 `skillKey` 字段
- `equipShip` / `repairShip` 改用 `calcCombatMaxHp()` 动态计算 HP
- 7 个计算函数：`calcPlayerHit` / `calcPlayerDmgMult` / `calcCombatMaxHp` / `calcPlayerDodge` / `calcFuelMult` / `calcRepairMult` / `calcCL`
- 旧存档 `gunnery` 自动迁移到 `laserOps` + `cannonOps`

---

### 二、侧边栏重构

#### 标签顺序重排
```
技能总览 → LP商店 → 仓库 → 船坞 → 动作队列 → [分割线]
→ 采矿 → 气体采集 → 行星开发 → [分割线]
→ 冶炼 → 舰船工程 → 弹药工程 → [分割线]
→ 战斗 + 10个战斗子技能 → [分割线]
→ 存档管理 → 设置
```

#### 等级显示
- 每个带技能对应的标签右侧显示 `Lv.X`，`renderSidebar()` 每秒刷新
- 鼠标悬停显示 `技能名 经验: 当前 / 升级所需`
- 战斗子技能标签全部 `data-skill="combat"`，点击均进入战斗面板
- "行星概览" 更名为 "行星开发"

---

### 三、战斗界面 HUD 重做

#### CSS 全面翻新
- 删除旧 `.battle-*` 全部样式（battle-row/enemy/player/consumables）
- `.combat-panel`：深空径向渐变 + 红褐边框 + `position:relative`
- `.combat-arena`：flex 左右布局，`.combat-player-side`（蓝底）左 VS `.combat-enemy-side`（红底）右，中间 `VS` 分割
- `.combat-side-image`：210×150 虚线边框占位区，后续放舰船图片
- `.health-bars-sm`：血条缩小至 6px 高、315px 宽居中
- 血条斜纹：`repeating-linear-gradient` 蓝/金/红三色
- 武器按钮 `.weapon-btn.active` 绿光、维修按钮 `.repair-toggle.on` 蓝光
- `body.in-combat::after`：120px 模糊暗红 inset box-shadow

#### JS 适配
- `renderCombatPanel`：去掉所有 emoji 拼接，ID 适配新结构，玩家命中/闪避动态显示
- `bindCombatButtons`：出击/停止切换 `body.in-combat` class

---

### 四、图片系统

- 新建 `images/ships/` 和 `images/enemies/` 目录
- 舰船数据加 `image` 字段：裂谷级 → `images/ships/裂谷级.png`
- 敌人数据加 `image` 字段：天使侦察兵 → `images/enemies/天使侦查舰.png`
- `spawnCombatEnemy` 传递 `image` 字段到 `currentEnemy`
- `renderCombatPanel` 动态检测 `image` 渲染 `<img>`，无图片时显示占位符

---

### 五、数值修正

- `SMELTING_RECIPES` 的 `baseXP` 之前是递减的（50→40→30→20→20→10→10），修正为与 `MINING_AREAS` 完全一致的递增序列（10→30→70→140→230→370→580）

---

### 六、战斗攻击特效

- 新增 `combat-fx-layer` 特效层，`position:absolute` 覆盖战斗面板
- `playAttackFX(isPlayer, weapon, dmg)`：光束 + 闪边 + 浮动伤害数字
  - 光束：`.fx-beam` + `beam-laser`(蓝) / `beam-cannon`(灰) / `beam-rocket`(橙)，从玩家区画到敌人区
  - 闪边：`.enemy-hit-flash`(红) / `.player-hit-flash`(蓝)，0.2s 边框高亮
  - 伤害数：`.fx-dmg` 红色 "−数值" 向上飘 0.6s 消失
- `combatTick` 在伤害穿透后调用，不碰 hp 赋值和公式
- 停止按钮清空 fx-layer

---

### 今日决策
- 战斗技能按 EVE 风格拆分为独立三系（激光/炮台/导弹），互不串门
- CL 取攻击防御各最高的平均值，鼓励专精
- 战斗子技能标签全部导向同一面板，面板内用 CL 摘要统一显示
- 图片用简单的 `image` 字段 + `<img>` 方案，不引入复杂资源管理
- 特效层用绝对定位 + `pointer-events:none`，不干扰交互

### 当前状态
- **已实现**：采矿、冶炼、气体采集、行星开发、舰船工程、弹药工程、仓库、存档、离线补算、确认弹窗、动作队列、船坞、**战斗技能全部生效**、**战斗 HUD**、**图片系统**、**攻击特效**
- **待实现**：装备工程、改装件工程、逆向工程、LP 商店、5 层安全等级、离线战斗、精英/Boss 敌人

### 明天计划
1. 安全等级扩展（0.7-0.5 / 0.4-0.3）+ CL 门槛
2. 或装备工程制造系统

## 2026-07-11（第 7 天）

### 代码规模
- **index.html**：4600 行 → 4763 行（**+163 行**，+3.5%）

---

### 一、新增系统（1 项）

#### 工业舰船系统（T1 层，参照策划案 §20260711细化）
- 新增 `INDUSTRIAL_SHIPS` 配置表（7 艘：冲锋者级/勘探者级实现 + 妄想级/采集者级/霍克级/奋进级/逆戟鲸级占位）
- 新增 2 个专属部件配方：`external_cargo`（外置货舱）、`industrial_mount`（工业挂架）
- 部件体系：与战斗舰共用 4 个部件（船体骨架/推进系统/核心系统/护盾发生器），仅替换装甲镀层和武器挂架
- 工业舰合成：`compCount: 8`（战斗舰 10），额外消耗类银超金属
- 蓝图商店新增：冲锋者级/勘探者级（50,000 ISK）

### 二、效率加成系统
- 新增 `getActiveIndustrialShip()` 辅助函数
- `getMiningEfficiency()` 加入工业舰采矿加成乘区（冲锋者+15% / 妄想+35% / 霍克+65% / 逆戟鲸+50%）
- `getSmeltingEfficiency()` 加入工业舰冶炼加成乘区（逆戟鲸+30%，其余为 0）
- `getGasEfficiency()` 加入工业舰采气加成乘区（勘探者+15% / 采集者+35% / 奋进+65% / 逆戟鲸+50%）

### 三、船坞双装备槽位
- 船坞页面新增"工业舰"类别标签和独立装备槽位（`activeIndustrialShip`）
- 战斗舰 → "设为战斗舰"按钮；工业舰 → "设为工业舰"/"卸下工业舰"按钮
- 新增 `isIndustrialShip()` / `getShipConfig()` / `equipIndustrialShip()` / `unequipIndustrialShip()`
- `getActiveShip()` 改为通过 `getShipConfig()` 查询，自动适配战斗/工业双表
- 舰船类型中文映射：`SHIP_TYPE_NAMES`（护卫舰/工业护卫舰/工业驱逐舰/工业巡洋舰/工业旗舰）

### 四、配套改动
- `gameState` 新增 `activeIndustrialShip: null`
- 存档迁移：旧存档自动补全 `activeIndustrialShip`
- `ITEM_ICONS` / `ITEM_CATEGORIES.equipment` / `SHIP_COMP_NAMES` 新增外置货舱和工业挂架

### 今日决策
- 部件同名（hull_frame等4个）消耗与战斗舰完全一致，以代码内配方为准
- 高级工业舰（妄想级及以上）部件配方暂占位，等待舰船制造体系扩展时统一实现
- 采气线差异化（额外气体消耗）暂不实现
- 工业舰蓝图暂时与战斗舰共用 ISK 蓝图商店，LP 商店留待后续

### 五、船坞行动适配系统（需求2）
- 废除旧的双字段（`combat.activeShip` + `activeIndustrialShip`），统一为 `shipAssignments: { 行动类型: 船ID }`
- 船坞卡片：每艘船底部显示 6 个可点击行动标签（战斗/采矿/采气/冶炼/舰船工程/弹药工程）
- 点击切换绑定，互斥规则自动生效（新船绑定同一行动→旧船自动解除）
- 状态栏显示 `📋 ⚔ 战斗+⛏ 采矿` 聚合标签
- 新增 `getAssignedShip(actionKey)` 辅助函数 → 效率函数改为从此读取加成

### 六、UI 细节优化（需求1 + 效率 hover）
- 侧边栏技能 hover：同时显示经验值 + 分隔线 + 技能描述
- 采矿/冶炼/采气效率值 hover：显示完整公式链（基础 × 等级 × 舰船加成）+ 来源船名

### 七、Bug 修复
- `getAssignedShip` 初始实现错误：`Object.entries` 遍历时 key/value 方向颠倒，`actions.includes()` 对字符串调用而非数组。修正为直接索引 `shipAssignments[actionKey]`。

### 遇到的问题
- `edit_file` 连续 4 次返回 "Search string not found"：原因是 `read_file` 显示内容与实际文件字节存在空格/缩进差异。**不应换工具硬刚**，应先 `python -c "print(repr(open('f').read()[N:N+200]))"` 查精确字节差异，修正搜索字符串后重试。
- 转用 `apply_patch` 时误传了 `changes` 参数：该参数会用给定内容**直接覆盖整个目标文件**（不是增量 patch），导致 index.html 被截断至 86 行。**教训：`apply_patch` 只用 `patch` 参数（标准 unified diff 格式），永远不碰 `changes`。**
- 被迫用 Python 脚本补救：`write_file` 写脚本 → `exec_shell` 执行，脚本内容本身大量消耗 token（尤其 `_patch_industrial2.py` 339 行）。**不到万不得已不写脚本文件。**
- **最终经验**：文件使用 CRLF 换行，`read_file` 显示为 LF，`edit_file` 多行搜索时 LF≠CRLF 导致失败。**单行搜索几乎 100% 成功**；大段替换时尽量拆成逐行改，或接受最小脚本桥接换行符差异。

### 当前状态
- **已实现**：采矿、冶炼、气体采集、行星开发、舰船工程、弹药工程、仓库、存档、离线补算、确认弹窗、动作队列、船坞、战斗技能全部生效、战斗 HUD、图片系统、攻击特效、**工业舰船（T1）**
- **待实现**：改装件工程、逆向工程、LP 商店、5 层安全等级、离线战斗、精英/Boss 敌人、高级工业舰解锁

## 2026-07-12（第 8 天）

### 代码规模
- **index.html**：4783 行 → 5049 行（**+266 行**，+5.5%）

---

### 一、工业舰属性重做（0712 效能加成版）

参照策划案 20260712 细化，工业舰不再直接提供采集速度加成，改为**装备效能加成**：
- `bonuses` 从 `miningEfficiency: 0.15` 等 → `miningLaserEfficiency: 1.0` 等
- 高槽装备基础加成 × (1 + 舰船效能)，冲锋者级效能 +100%、霍克级 +200%、逆戟鲸级 +280%
- `slots` 更新为 0712 版配置（冲锋者 高2/中2/低1，逆戟鲸 高4/中4/低3 等）
- 冶炼效率加成（逆戟鲸 +30%）保持不变，冶炼无装备槽位

### 二、装备系统 T1 层

#### 装备数据库
- 新增 `EQUIPMENT_DB`：6 种 T1 装备（采矿激光器、气云采集器、无人机控制单元、小型护盾扩展、采矿提升器、采气提升器）
- 高槽决定"产什么"，低槽决定"产多少"，中槽护盾扩展提供生存

#### 装备工程面板
- 侧边栏新增 🔧 装备工程入口（`equipmentEngineering` 技能）
- 面板：下拉配方选择、材料库存显示、Canvas 进度条、开始/停止制造
- 弹药工程风格对齐（`ship-progress-row` + `drawSkillBar` 紫色）

#### 效率函数重写
- `getMiningEfficiency` / `getGasEfficiency` 接入高槽+低槽装备乘区
- 高槽：遍历 `fitted.high`，每个装备加成 × (1 + 舰船效能)
- 低槽：独立乘区 `(1 + lowSlotBonus)`
- 冶炼效率：仅逆戟鲸级提供 +30%，无装备乘区

#### 全链路集成
- gameTick：装备工程制造循环（材料检查→扣除→产出→进度推进）
- 离线补算：离线收益自动计算装备工程产出
- 确认弹窗：显示材料需求、库存、批量执行次数
- 队列系统：`addCurrentToQueue` + `ACTION_LABELS` 扩展
- 存档迁移：旧存档自动补全 `gameState.equipment` + `equipEngTarget`

### 三、舰船装备环带弹窗

- 27 段固定 SVG 环带：高槽 0-7（红）、中槽 8-15（蓝）、低槽 16-23（金）、改装件 24-26（紫/灰）
- 超出舰船 slots 配置的槽位变灰（opacity 0.25）+ `pointer-events: none`
- 点击槽位 → 弹出装备选择面板（从 inventory 筛选匹配槽位类型的已制造装备）
- 卸下装备 → 归还 inventory，fitted 对应位置清空
- `syncOrbitToFitted()`：装卸后自动同步到 `gameState.equipment.fitted`
- 船坞每个舰船卡片新增 🔧 装备按钮
- 属性栏四色（护盾蓝/装甲金/结构红/速度绿）与船坞卡片同步

### 四、Bug 修复（7 项）

| # | Bug | 修复 |
|---|-----|------|
| 1 | 装备工程下拉菜单不工作 | `custom-select` → `area-dropdown`，`dropdown-content` → `area-dropdown-content` |
| 2 | 装备工程面板页面切换后残留/再进不去 | `switchSkill` 选择器和隐藏列表加入 `#equipeng-panel` |
| 3 | 装备工程点开始无反应 | `showActionConfirm` 中 equipmentEngineering 分支被错误写成面板切换代码，修正为确认弹窗 |
| 4 | `switchSkill` 中 equipmentEngineering 混入弹窗代码导致 ReferenceError | 替换为面板显示逻辑（显示 equipeng-panel + renderEquipEngPage） |
| 5 | 装备制造进度条不动 | `renderEquipEngPage` 补充 pct/etaText 计算和 `drawSkillBar` |
| 6 | `renderLoop` 缺少装备工程进度条平滑更新 | 补上 `bar-equipeng` + `equipeng-eta` 更新 |
| 7 | 侧边栏未高亮 + 活动指示器缺失 | 给 `icons` 和 `actEl` 补上 `equipmentEngineering` 映射 |

### 今日决策
- 工业舰效率模型：舰船只放大装备效果（效能加成），无装备时效率等于裸奔
- 装备系统分两期：本期先做 T1 基础装备+制造面板，装配 UI（环带）提前做完；装备工程自身无舰船加成
- 环带固定 27 段，不动态生成槽位数，超出部分变灰
- 改装件槽位（3 段）暂不开放
- 采气线差异化（替换部件额外气体消耗）暂不实现

### 遇到的问题
- CRLF 换行导致 `edit_file` 多行搜索几乎 100% 失败，**单行搜索 + Python 脚本桥接是最可靠的修改方式**
- `apply_patch` 的 `changes` 参数会直接覆盖目标文件（教训：只用 `patch` 参数）

### 当前状态
- **已实现**：采矿、冶炼、气体采集、行星开发、舰船工程、弹药工程、**装备工程（T1）**、仓库、存档、离线补算、确认弹窗、动作队列、船坞、战斗技能全部生效、战斗 HUD、图片系统、攻击特效、工业舰船、**舰船装备环带弹窗**
- **待实现**：装备装配到工业舰效率系统、T2/T3 装备、改装件工程、逆向工程、LP 商店、5 层安全等级、离线战斗、精英/Boss 敌人、高级工业舰解锁