# 深空放置：边疆纪元 — TapTap RC7 更新说明

本次更新大幅强化了存档安全，并接入了 TapTap 平台能力。

## 新增内容

1. **TapTap 云存档**
   新增云端存档。你的进度可上传至 TapTap 云端，换设备或清理本地数据后都能取回。

2. **设备双代备份**
   新增设备本地双代备份（当前代 + 上一稳定代）。当同一设备的本地缓存异常、损坏或被清理时，可自动回退到上一稳定备份，减少设备端丢档风险。

3. **本地 / 设备备份 / 云端 三层存档保护**
   存档现在由三层共同保护：
   - **本地**：浏览器内的主存档。
   - **设备备份**：同一设备上的双代本地备份，应对本地缓存异常。
   - **云端**：TapTap 云端存档，应对换机、清数据等场景。
   任意一层异常时，系统会优先从可用层中安全恢复，绝不静默开新档。

4. **本地 / 云端冲突选择与双方备份导出**
   当本地存档与云端存档不一致时，会弹出冲突选择框，你可以明确选择「使用本地」或「使用云端」，并可在操作前导出双方备份，避免误操作导致进度丢失。

5. **存档管理状态与操作**
   存档管理界面新增：
   - **立即同步**：手动把当前进度推送到云端。
   - **设备备份**：查看设备双代备份状态与执行备份。
   - **检查云端**：对比本地与云端，确认云端存档是否最新。
   界面会实时显示同步状态、上次备份时间、设备备份状态与云端状态。

6. **云端同步限频，连点不丢档**
   云端同步限制为**每分钟最多一次**。连续点击「立即同步」只会提示「操作频繁」，不会触发重复上传或覆盖，也不会造成丢档。

7. **「永久删除」不可恢复**
   「永久删除」会同时删除**本地存档、设备备份与云端存档**，删除后**无法恢复**。请谨慎操作，删除前建议先确认已无需要保留的进度。

8. **无法找回历史丢失的存档**
   本功能上线前已经丢失、且从未上传过云端的存档，无法被本次更新找回。新保护仅对更新后产生的存档生效。

## 关于成就

本次**提交通用了成就同步的底层架构与映射配置**，但 TapTap 后台成就内容仍在配置中，**成就同步当前默认关闭（全部 `enabled=false`），TapTap 成就尚未正式上线**。
后续后台成就配置完成后，将随新版本开启，无需再次下载客户端。
